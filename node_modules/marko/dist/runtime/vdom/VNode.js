/* jshint newcap:false */
function VNode() {}

VNode.prototype = {
    bs_: function (finalChildCount) {
        this.bM_ = finalChildCount;
        this.bN_ = 0;
        this.bA_ = null;
        this.bO_ = null;
        this.bx_ = null;
        this.by_ = null;
    },

    bl_: null,

    get a_() {
        var firstChild = this.bA_;

        if (firstChild && firstChild.bz_) {
            var nestedFirstChild = firstChild.a_;
            // The first child is a DocumentFragment node.
            // If the DocumentFragment node has a first child then we will return that.
            // Otherwise, the DocumentFragment node is not *really* the first child and
            // we need to skip to its next sibling
            return nestedFirstChild || firstChild.b_;
        }

        return firstChild;
    },

    get b_() {
        var nextSibling = this.by_;

        if (nextSibling) {
            if (nextSibling.bz_) {
                var firstChild = nextSibling.a_;
                return firstChild || nextSibling.b_;
            }
        } else {
            var parentNode = this.bx_;
            if (parentNode && parentNode.bz_) {
                return parentNode.b_;
            }
        }

        return nextSibling;
    },

    bj_: function (child) {
        this.bN_++;

        if (this.bG_ === true) {
            if (child.bP_) {
                var childValue = child.bt_;
                this.bE_ = (this.bE_ || "") + childValue;
            } else {
                throw TypeError();
            }
        } else {
            var lastChild = this.bO_;

            child.bx_ = this;

            if (lastChild) {
                lastChild.by_ = child;
            } else {
                this.bA_ = child;
            }

            this.bO_ = child;
        }

        return child;
    },

    bH_: function finishChild() {
        if (this.bN_ === this.bM_ && this.bx_) {
            return this.bx_.bH_();
        } else {
            return this;
        }
    }

    // ,toJSON: function() {
    //     var clone = Object.assign({
    //         nodeType: this.nodeType
    //     }, this);
    //
    //     for (var k in clone) {
    //         if (k.startsWith('_')) {
    //             delete clone[k];
    //         }
    //     }
    //     delete clone._nextSibling;
    //     delete clone._lastChild;
    //     delete clone.parentNode;
    //     return clone;
    // }
};

module.exports = VNode;